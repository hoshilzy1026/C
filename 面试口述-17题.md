# 面试口述稿（17 题 · 口语化）

> 用法：对着镜子把每题「开场」说一遍，卡住再看「展开」。别逐字背，面试官听的是你能不能用自己的话把因果讲圆。
> 原则：先结论，再机制，最后一句落到你做过的事上。被追问再往下挖，别一上来把细节全倒出来。

---

## 1. 解读一下 shared_ptr

**开场（20 秒）**

`shared_ptr` 就是好几个指针一起管同一块堆内存，谁都有份，最后一个走的人负责把它析构掉。底层不是魔法，就是「对象指针 + 控制块」这两件事。

**展开**

一个 `shared_ptr` 对象里其实有两个指针：一个指向真正的对象，一个指向堆上的控制块。控制块里最关键的是两个计数——强引用计数和弱引用计数，还有删除器。你拷贝一份 `shared_ptr`，强计数原子加一；析构一份，原子减一。强计数掉到 0，对象销毁；弱计数也掉到 0，控制块这块内存才真正释放。所以对象死了和控制块释放，是两个时刻，别混。

`make_shared` 比 `new` 再塞进 `shared_ptr` 好在一次分配：对象和控制块贴在一块儿，局部性更好，也少一次堆分配。坑是：只要还有 `weak_ptr` 活着，整块内存（包括已经析构掉的对象那块空间）都还不回去。大对象加长寿 weak，内存会拖着不放。

线程安全这块最容易答错，分三层说：

1. 控制块的计数操作是原子的，多个线程各自拿着自己那份 `shared_ptr` 拷贝、销毁，没问题。
2. 指向的那个对象本身，`shared_ptr` 不管同步，多线程读写对象你自己加锁。
3. 多个线程同时改**同一个** `shared_ptr` 实例（比如一个全局的，一个线程 `reset`，另一个线程拷贝），这是数据竞争。C++20 才有 `atomic<shared_ptr>` 专门管这个。

循环引用是经典坑：A 持 B、B 持 A，强计数永远不为 0。反向边改成 `weak_ptr`。异步回调里要保活对象，标准姿势是继承 `enable_shared_from_this`，lambda 里捕获 `shared_from_this()`，别自己 `shared_ptr<T>(this)`，那会造出第二套控制块，析构两次直接炸。

**被追问就补一句**

别名构造（aliasing constructor）是为什么对象指针要单独存、不能从控制块推出来的原因：我可以指向对象的某个成员，但生命周期跟整个对象绑在一起。

---

## 2. map 和 unordered_map

**开场**

一句话：`map` 是红黑树，有序，最坏也是 O(log n)；`unordered_map` 是哈希表，平均 O(1)，最坏能退化成链表。点查冲吞吐用哈希，要范围查询、要有序、要对延迟有硬保证，用 `map`。

**展开**

`map` 底层红黑树。节点式存储，插删只动被删那个节点的迭代器，别的迭代器、指针、引用都还活着。它能做 `lower_bound`、前驱后继、按 key 区间扫，这些哈希表做不了，除非你自己再维护一套顺序。红黑树选它而不是 AVL，是因为读写混合时旋转更少，插入最多两次旋转，当通用容器更合适。

`unordered_map` 标准库实现是开链：桶数组 + 冲突链表。hash 取桶，桶里再比 `==`。装载因子默认到 1 就 rehash，桶大概翻倍，所有元素重新分桶。rehash 之后**迭代器全失效**，但元素的指针和引用还在——节点没搬，只是换了条链。哈希质量不行、或者被人专门撞哈希，会退化成 O(n)，对外部输入做 key 要想这件事。

怎么选，我实际就这几条：

- 纯 `find(key)`、不在乎顺序 → `unordered_map`，预知规模先 `reserve`，少 rehash。
- 要有序遍历、范围查、延迟敏感（哈希偶发 O(n) 扛不住）→ `map`。
- 两者节点都碎，缓存不友好。热路径上我们更愿意用开放寻址的第三方表，比如 absl 那种 flat hash，但面试先把标准库讲清楚就行。

自定义类型当 key：哈希函数和相等比较必须一致——相等的对象哈希必须相同，反过来不要求。漏写其中一个，编译过了运行期就是随机 bug。

---

## 3. vector 底层如何实现

**开场**

`vector` 就是一块连续内存，外面三个指针：头、当前尾、容量尾。看起来像数组，关键戏在扩容。

**展开**

`size` 是用了多少，`capacity` 是这块内存能装多少。`push_back` 容量够就直接在尾部构造；不够就扩容：申请一块更大的，把旧元素搬过去，旧的析构掉、内存释放。libstdc++ 一般按 2 倍涨，MSVC 按 1.5 倍。2 倍均摊是 O(1)，但旧块面积加起来永远拼不回新块那么大，内存不太好复用；1.5 倍有机会复用，常数稍差。面试说到「几何增长保证均摊 O(1)」就够了。

搬的时候有个细节：元素的移动构造如果标了 `noexcept`，扩容就移动；否则为了强异常安全——搬到一半抛了，旧数据还得完好——会退化成拷贝。所以你自己写的类型，移动构造记得标 `noexcept`，不然 `vector` 扩容等于白优化。

工程上已知大小先 `reserve`，别让它扩三次搬三次。`shrink_to_fit` 只是请求收缩，不保证立刻还内存。扩容之后，所有迭代器、指针、引用全部失效，保存 `&v[i]` 再 push，是经典踩坑。

`push_back` 和 `emplace_back`：前者塞一个已经造好的对象（拷或移），后者把参数完美转发，在容器内存上原地构造。从参数现场造对象，emplace 更干净；已经有一个现成对象，两者差不多。emplace 会绕过 `explicit`，这是个小坑。

---

## 4. 解读内存原子序

**开场**

单线程你写的代码，编译器和 CPU 怎么重排结果都一样；多线程就不一样了。内存序就是告诉编译器和 CPU：「这两步之间，别的核必须按这个顺序看见」。C++ 给了六种，日常真正用的就三种：relaxed、acquire/release、seq_cst。

**展开**

先说为什么需要。核有自己的 store buffer，你写完了，别的核不一定立刻看见。x86 还算强（TSO，主要是 StoreLoad 能重排），ARM 更弱。C++ 内存模型的契约是：两个线程无同步地碰同一块内存且至少一个是写，叫数据竞争，直接 UB，不是「读到旧值」那么温柔。

六种可以这么记：

- **relaxed**：只保证这一次操作本身原子，跨线程不建立顺序。适合统计计数器，最后读一次总数。
- **release（写）**：我这次写之前的所有写，都不能排到这次写后面。意思是「发布：我前面干的活，跟这次写绑在一起出去」。
- **acquire（读）**：我这次读之后的访问，不能排到这次读前面。意思是「拿到锁/看到标志之后，前面发布的东西我都能看见」。
- 配对：A 线程 release 写一个标志，B 线程 acquire 读到了这个新值，两边就 synchronizes-with。这是理解无锁的钥匙。
- **acq_rel**：CAS、`fetch_add` 这种读改写，同时具备两种语义，用来接力传递。
- **consume**：理论上只保数据依赖链，比 acquire 轻，但编译器搞不定，实践里当 acquire 用，别主动用。
- **seq_cst**：默认值。在 acquire/release 之上再加一个全局总顺序，所有线程看到的 seq_cst 操作顺序一样。Dekker 那种「两个线程各写一个变量再读对方」只有它能保证不出现双方都读到旧值。弱内存架构上屏障更重。

经典用法就这一套：生产者先把数据写好（普通写就行），再 release 写一个 ready 标志；消费者 acquire 读到 true，再去读数据，保证完整。数据本身甚至都不用原子。无锁队列、双检锁，全是这个变体。两边都不能省：生产者省 release，数据可能排到标志后面才写；消费者省 acquire，可能先读数据、后看标志，读到半成品。

日常建议：先全部 `seq_cst`，对了再在热路径上收成 acquire/release。别一上来满地 relaxed，自己都讲不清 happens-before。

---

## 5. AIO 和 io_uring 怎么做的，有什么区别

**开场**

Linux 上两代「真异步 IO」。老 AIO（libaio）是给磁盘设计的，坑很多；io_uring 几乎是逐条把那些坑补掉的。核心差别：AIO 每次提交/收割都是系统调用，而且很多场景会退化成同步；io_uring 是用户态和内核共享一对无锁环，可以一批一批交、一批一批收，甚至稳态下零系统调用。

**展开**

先把同步/异步说清楚。读一个 socket 两段：等数据到（就绪），再把数据从内核拷到用户态。epoll 管的是第一段，数据还得你自己 `read`；AIO/io_uring 是两段都交给内核，完成通知来的时候数据已经在你的缓冲区里了。所以 epoll 是 Reactor（就绪通知），io_uring 是 Proactor（完成通知）。

**Linux AIO**：`io_setup` 建上下文，`io_submit` 交一批 `iocb`，`io_getevents` 收完成。听着很美，实际限制很狠：基本只有 `O_DIRECT` 文件才真异步，buffered 下 `io_submit` 经常自己同步做完；就算 Direct IO，元数据没就绪、设备队列满，提交本身也可能阻塞；不支持网络 socket；每次提交收割都是 syscall。所以生产上基本是数据库这种完全自己管 IO 模式的在用，InnoDB 就是典型。

**io_uring**：mmap 出来两块共享内存——提交队列 SQ、完成队列 CQ。你在 SQE 里填操作码、fd、缓冲区、偏移，推尾指针，一次 `io_uring_enter` 可以交一批、顺手收一批 CQE。两端用原子游标同步，本质就是内核和用户态之间的一对 SPSC 环。进阶可以说：

- SQPOLL：内核起个轮询线程盯着 SQ，你只管往共享内存写，稳态提交路径零 syscall。
- 注册 buffer/file：提前 pin 住，省每次地址检查。
- LINK：把「先写再 fsync」串成依赖链。
- 文件、网络、元数据操作统一接口，buffered 也能真异步（内核用 io-wq 线程池兜那些内核不了的操作）。

对比收口：AIO 是「专用、易退化、syscall 密」；io_uring 是「通用、批量、共享内存、可零 syscall」。和我们做存储的关系：用户态 SPDK 连 io_uring 都绕开了，直接轮询 NVMe 队列，连中断都没有。io_uring 是内核路径里最接近这个模型的东西。

---

## 6. Go 存在内存泄漏的情况，如何解决

**开场**

Go 有 GC，几乎没有 C 那种「忘了 free」。面试里说的泄漏，本质是**还有引用，GC 不敢收**。最常见两类：goroutine 泄漏，和堆上的 map/slice/cache 只涨不缩。

**展开**

goroutine 泄漏：G 卡死在 channel 或锁上，它栈上、闭包里抓着的对象全活着。典型剧本：

- 往一个没人收的 channel 里发，接收方提前走了。
- 从一个没人发的 channel 里收，生产者挂了还没 close。
- `for { select { ... } }` 缺退出分支。
- 建了 context 忘了 cancel，底下派生的监听 G 一直挂着。
- 死锁。

堆泄漏：往 map 里塞东西不删；slice 引用了一块大数组的一小段，整块底层数组活着；全局 cache 没有淘汰；time.Ticker 没 Stop。

怎么查：先看 goroutine 数量是不是只涨不回。开 `net/http/pprof`，heap 看 `inuse_space`（此刻还活着的），别看 `alloc_space`（那是累计分配）。隔一两分钟采两份，`go tool pprof -base old new` 看增量。goroutine profile 直接能看见这帮人卡在哪一行。

怎么修：

- 谁创建谁负责收尾。channel 由发送方 close；所有可能阻塞的收发都加 `select { case ...: case <-ctx.Done(): }`。
- context 贯穿调用链，超时、取消一条路走到底。
- map 要有淘汰或定期重建；slice 如果只留尾巴，`copy` 到新 slice 把大底层数组放开。
- 修完用同一负载再采一次，`inuse_space` 和 goroutine 数要回到稳态，别看一次 GC 后的瞬时值就说好了。

我们导入导出那块就踩过：双缓冲流水线出错时，要先 close abort 再把 filled 排空，少一步 reader 就堵在 channel 上，goroutine 泄漏。面试可以把「每个阻塞点被谁解锁」讲清楚，比空谈 pprof 有说服力。

---

## 7. 解读 lambda 表达式

**开场**

lambda 不是函数，是编译器给你现场捏的一个匿名类。捕获的变量变成这个类的成员，函数体变成 `operator()`。所以它是个对象，有状态。

**展开**

无捕获的 lambda 没状态，可以隐式转成普通函数指针，所以能塞给 C 风格回调。有捕获就不行。`operator()` 默认是 const 的，值捕获的变量在里面改不了，要改加 `mutable`。

捕获怎么选，核心就一个字：生命周期。

- `[=]` 值拷一份，安全但有拷贝成本；`[&]` 引用，便宜但异步场景是重灾区——lambda 扔进线程池，执行的时候局部变量栈已经没了，悬垂。
- `[=]` 看起来什么都拷了，但捕获 `this` 时拷的是指针本身，对象析构后访问成员照样炸。异步回调里访问成员崩溃，根因经常在这。C++17 用 `[*this]` 把整个对象拷进去。
- C++14 初始化捕获：`[p = std::move(ptr)]`，能把 `unique_ptr` 移进闭包，异步任务传递独占所有权全靠它。

和 `std::function` 的差别：lambda 类型编译期就定了，调用能内联，零开销；`std::function` 是类型擦除，可能堆分配，还有一次间接跳转。IO 热路径回调用模板或 `auto` 接 lambda，别包一层 `std::function`。Seastar 的 `future::then` 就是这么干的。

---

## 8. C++20 协程

**开场**

C++20 协程是无栈的。编译器把带 `co_await` / `co_yield` / `co_return` 的函数改写成一个状态机，局部变量和挂起点塞进协程帧（默认堆上，能证明生命周期短可以优化掉，叫 HALO）。挂起就是保存状态、把 CPU 还给调用方或调度器；恢复从断点接着跑。

**展开**

语言只给机制，行为两个定制点说了算：

- **promise_type**：这个协程整体怎么表现——一开始挂不挂、最后挂不挂、返回值怎么产生、异常怎么处理。
- **awaiter** 三个函数：`await_ready` 问要不要挂；`await_suspend` 挂了之后拿着协程句柄去登记「啥时候唤醒我」，比如注册到 IO 完成回调；`await_resume` 给出 `co_await` 表达式的值。

跟线程比：线程内核抢占，切换微秒级，栈默认 8MB；协程用户态协作，切换纳秒到百纳秒，可以百万级。跟 Go 的 goroutine 比：Go 是有栈协程，任意调用深度都能挂，对业务代码透明，代价是每 G 一块可增长栈；C++20 是无栈，挂起只能写在协程函数体内，普通函数不能 `co_await`，有函数染色问题，但帧更小、更好优化。

跟简历怎么接：Seastar 早期用 future/then 链，手写状态机，回调套回调很难看。C++20 协程是同一套 reactor 上的语法糖，`co_await` 一个 future，底层调度没变，代码变成顺序的。存储这种「海量小请求、满地 IO 等待」的场景，协程比线程池更对口。

---

## 9. 平常用哪些锁，怎么用，如何选择

**开场**

我日常就这几把：`mutex` + `lock_guard` 是默认；读多写少才考虑 `shared_mutex`；跟条件变量配合必须用 `unique_lock`；多把锁一起拿用 `scoped_lock`。能不用锁就不用——队列解耦、按核分片，比把锁调得更细更有效。

**展开**

`mutex` 在 Linux 上是 futex：没人抢，就是用户态一次原子操作，很便宜；有人抢，才陷入内核睡觉。所以「轻度竞争的互斥锁」没有很多人以为的那么贵。

`recursive_mutex` 允许同一线程反复加。我基本不用，看到它通常说明锁的边界没划清，调用链自己锁自己。

`shared_mutex` 读写锁：多个读者、一个写者。读多写少、临界区还不算短，才值得。临界区就几条指令，或者写也不少，普通 mutex 往往更快——读写锁自己还要维护计数。另外要小心写者饥饿。

三个守卫：

- `lock_guard`：进门上锁、出门开锁，不能提前解锁，最轻，默认用它。
- `unique_lock`：能 defer、能 try、能中途 unlock，条件变量的 `wait` 必须配它，因为等待期间要先放锁。
- `scoped_lock`：一次拿多把，内部用避免死锁的算法，别手写「先 A 后 B」。

怎么选，我的顺序：

1. 先问能不能不要共享。Seastar / DAOS target 那种 per-core 私有数据，跨核丢消息，数据路径上零锁。
2. 必须共享、临界区短、竞争不重 → `mutex`。
3. 读多写少、临界区有点分量 → 再测读写锁，别想当然。
4. 只保护一个计数、标志 → 原子变量，别上锁。
5. 生产者消费者 → mutex + condvar，或者直接无锁队列（见第 13 题）。

死锁四条件：互斥、持有并等待、不可剥夺、循环等待。工程上最好让一个线程同时只持一把；必须多锁就 `scoped_lock` 或全局约定顺序。线上死了，gdb `thread apply all bt`，找停在 futex_wait 的，交叉对 owner。

---

## 10. RAII 思想

**开场**

RAII 就是：资源的生命跟一个栈对象绑死。构造时拿到，析构时还掉。C++ 析构是确定的，return、异常、怎么跳出作用域，析构都会按构造的反序跑。所以它是异常安全的底盘。

**展开**

资源不只是内存，锁、fd、连接、事务、mmap，凡是「成对出现的获取/释放」，都该包成一个守卫类。标准库里一堆都是：`lock_guard`、`unique_ptr`、`fstream`、`jthread`。

为什么重要：手写 `lock(); ... unlock();`，中间 return 或者抛异常，锁就悬着。RAII 把所有退出路径一次覆盖。异常安全三个档：基本保证（不泄漏、状态合法）、强保证（失败像没发生过，典型 copy-and-swap）、不抛保证（析构、swap 该达到）。没有 RAII，这三档都谈不上。

跟智能指针、锁守卫是同一件事的不同实例。我们控制面做复合操作（daos / dmg / SPDK 三层编排），失败要分阶段回滚，思想一样：每个阶段的副作用绑在一个生命周期上，出错沿原路拆掉，不留半成品。

---

## 11. 智能指针有什么，实际开发中如何选择

**开场**

常用就三个：`unique_ptr` 独占，`shared_ptr` 共享，`weak_ptr` 旁观。选择的第一原则是**先表达所有权，再考虑性能**。默认 `unique_ptr`，真的要共享再上 `shared_ptr`，反向引用用 `weak_ptr`。

**展开**

`unique_ptr`：不能拷，只能移。没自定义删除器时大小就是一个裸指针，零开销。工厂函数返回它，意思就是「所有权交给你」。数组有 `unique_ptr<T[]>`。需要自定义删除器（比如关 fd、调 C 库 free）也能挂上，删除器是类型的一部分。

`shared_ptr`：见第 1 题。有控制块、原子计数，不是免费的。拷贝一次就是一次原子加。适合「很多地方都要保活同一对象」，比如异步回调期间对象不能死。能 unique 就别 shared。

`weak_ptr`：不增加强计数，`lock()` 成功才拿到一份临时的 shared。用来破循环、做观察、做缓存：「我关心你，但不负责让你活着」。

怎么选，落地几条：

- 函数只是用一下、不参与生命周期 → 参数写 `T&` 或 `T*`，不要 `const shared_ptr<T>&`。后者既强迫调用方必须用 shared 管，语义还含糊。
- 函数要留下所有权 → 按值传 `unique_ptr` 或 `shared_ptr`。
- 类成员：这个类拥有它 → `unique_ptr`；和别人共有 → `shared_ptr`；只是指回去（子指父）→ 裸指针或 `weak_ptr`。
- 异步 lambda 要保活 → `shared_from_this()`，不要捕获 `this`。
- 别用已经废弃的 `auto_ptr`。

一句话收口：智能指针表达所有权，裸指针/引用表达借用。满地 shared 往往是没想清楚谁该负责释放。

---

## 12. 迭代器失效的情况

**开场**

失效规则跟容器底层怎么存数据直接相关：连续内存一搬，迭代器指针引用全完；节点式的，只废被删的那一个。面试按容器背一张表就稳。

**展开**

**vector / string**

- 扩容：全部迭代器、指针、引用失效。
- 没扩容，中间插入或删除：插入/删除点及之后的失效，前面的还在。

**deque**

- 中间插删：全失效。
- 头尾插入：迭代器全失效，但引用还在（元素没搬，迭代器结构变了）。
- 头尾删除：只有被删那个失效。

**list / forward_list**

- 只有被删元素的迭代器失效，插入不影响任何人。

**map / set 及 multi 系列**

- 只有被删节点失效，其他都稳。这是节点式容器的好处。

**unordered_map / unordered_set**

- rehash：所有迭代器失效，引用不失效。
- 没 rehash 的插入：迭代器还有效。

遍历中删除，正确写法是 `it = c.erase(it)`，用 erase 的返回值。先 erase 再 `++it` 是未定义。vector 从后往前删、或者先记要删的再统一删，也行，别一边正向扫一边 erase 中间元素还自己 ++。

保存 `vector` 里元素的地址、引用，再 `push_back`，是线上最常见的失效 bug。已知要涨，先 `reserve`。

---

## 13. 无锁队列怎么实现的

**开场**

先把范围说小：我能手写、工程里真正常用的，是 SPSC——单生产者单消费者。MPMC 要处理 ABA 和内存回收，自己造很容易出幽灵 bug，生产上用成熟实现。

**展开**

SPSC 就一个环形缓冲区加两个原子游标。head 只由消费者推，tail 只由生产者推。生产者把数据写进槽位，再 **release** 推进 tail；消费者 **acquire** 读 tail，发现有数据再读槽。因为每个游标只有一个写者，不需要 CAS，纯 load/store 就正确，这是它快的根源。

优化两点：head 和 tail 各占一个缓存行，避免伪共享；本地缓存一份对端游标，减少原子读。满和空的判断：tail + 1 == head 为满（浪费一个槽），或者另外用 size 计数，各有取舍。

MPMC 难在：多个人抢同一个槽要用 CAS；CAS 成功不代表你看到的还是当初那个节点（ABA）；弹出的节点什么时候能真正 free（hazard pointer、epoch reclaim）。面试讲到「我知道坑在哪，所以不会在生产里从零造 MPMC」是加分的。

落到项目上：Seastar 跨核通信就是每对核一条 SPSC；io_uring 的 SQ/CQ 也是内核和用户态的一对 SPSC。DAOS 每个 target 绑一个核、数据私有，跨核走消息而不是共享队列。高性能系统的通用手法就一句话：识别出单写者结构，用 acquire/release 代替锁。

---

## 14. Direct 写的方式会怎么样

**开场**

`O_DIRECT` 写，绕过 page cache，用户缓冲区和块设备直接 DMA。好处是不双缓存、IO 大小和落盘时机自己说了算；代价是对齐约束、失去内核预读回写、写成功不等于持久。

**展开**

buffered 写：`write` 返回只表示进了 page cache，脏页什么时候刷是内核的事。读有预读，热数据二次访问走内存，对通用程序很友好。

Direct 写：

- 缓冲区地址、文件偏移、长度，都要按逻辑块对齐（一般 512 或 4K），不对齐直接 `EINVAL`。
- 绕过 page cache：导 100GB 镜像不会把整机 cache 冲光；自己管缓存的系统（数据库 buffer pool、存储引擎）避免和内核各缓存一份。
- 内核不再帮你合并、预读，小 IO 会很难看，所以 Direct 路径通常自己拼大块、自己对齐。
- **Direct ≠ 持久化**。数据到了设备缓存还可能掉电丢失，该 `fsync` / FUA / 盘带电容（PLP）还得有。我们生产盘要求 PLP，就是这个原因。

我们导入导出就是这么用的：NVMe-oF 块设备侧开 `O_DIRECT`，本地文件侧保持 buffered。设备侧绕 cache、不污染；文件侧留着文件系统预读，也避免有的文件系统根本不支持 Direct。对齐三件事拆开做：切分范围按 4K 对齐偏移；尾巴长度补零凑齐；缓冲区用匿名 mmap 保证地址对齐。源读不满一块就放弃 Direct 退回 buffered，目标可以补零，所以目标侧 Direct 保得住。

SPDK / DAOS 数据路径更彻底：用户态直接对 NVMe 下发，连 `O_DIRECT` 这层文件系统接口都没有。`O_DIRECT` 是「还走内核块层、但不要 page cache」的折中；用户态驱动是「连内核块层也不走」。

---

## 15. select / poll / epoll / AIO / io_uring

**开场**

这是一条从「我帮你看哪些 fd 就绪了」到「我帮你把 IO 做完」的演进。前三个是 IO 多路复用，等就绪；AIO 和 io_uring 是异步完成。规模越大、越要批量，越往后靠。

**展开**

**select**：fd 集合是固定大小位图，历史上 1024 上限。每次调用把整集合拷进内核，内核线性扫，返回后再自己扫一遍谁就绪，而且集合被改掉了每次还得重建。三重 O(n)。

**poll**：换成数组，去掉数量上限，其余毛病还在——每次传全量、内核扫全量。

**epoll**：内核里维持一个实例，关注列表用红黑树（或等价结构）管，`epoll_ctl` 注册一次就行。fd 就绪时中断回调把它挂到就绪链表，`epoll_wait` 只拷就绪的那些。成本跟就绪数有关，跟关注总数无关。所以万级长连接、大部分在睡，epoll 碾压；就几十个 fd 全活跃，三者差不多。

epoll 两种触发：LT 水平——缓冲区还有数据就一直通知，漏读下次还会来，好写；ET 边缘——只在「无变有」那一下通知，必须非阻塞循环读到 EAGAIN 抽干，否则这连接就僵死。写事件 LT 下「还能写就一直报」，所以不能常驻 `EPOLLOUT`，写不完再注册、写完摘掉。

**AIO / io_uring**：见第 5 题。补一句跟 epoll 的关系：io_uring 能覆盖 epoll 做的事，而且是完成通知，还能批量、能注册 buffer。新项目如果内核够新，网络加磁盘可以统一走 io_uring；存量高并发服务器 epoll 仍然是主流，没必要为了换而换。

串起来记：

| 模型 | 等什么 | 数据谁搬 | 典型场景 |
|------|--------|----------|----------|
| select/poll | 就绪 | 应用 read | 小规模、移植 |
| epoll | 就绪 | 应用 read | 海量连接 |
| Linux AIO | 完成（磁盘） | 内核 DMA | 数据库 Direct IO |
| io_uring | 完成（文件+网络） | 内核 | 新一代统一异步 |
| SPDK | 完成（用户态轮询） | 用户态驱动 | 存储引擎数据路径 |

---

## 16. DAOS 如何实现快照，如何实现回滚

**开场**

DAOS 的快照不是拷数据，是在 MVCC 上钉一个 epoch。回滚也不是把旧数据抄回来，是把这个 epoch 之后的版本扔掉。跟 Ceph RBD 那种对象级 COW 是两条路：RBD 是「共享基线 + 写时分裂」；我们是「天然多版本 + 标记保护 / 丢弃未来」。

**展开 · 快照**

VOS 里每次更新都带着 epoch（HLC 混合逻辑时钟），同一 key 多个版本共存，读可以指定读哪个时刻的视图。打快照三步：

1. 容器服务（MS，Raft）分配一个 epoch；
2. 写进 RDB 里这个容器的快照表 `c_snaps`；
3. 广播到每个 engine，更新内存里的快照列表。

后台聚合回收旧版本的时候，会绕开被快照引用的 epoch。所以打快照是纯元数据，秒级，零拷贝。我们改了一处：创建快照时带上 OIT（Object Index Table）——这个 epoch 下全部对象 OID 的索引，后面从历史快照克隆才能把对象枚举出来。

克隆（简历上的能力）可以顺口带一句：上游只能克隆「当前状态」；我们加了 `--epc`，打开那个快照的读事务，按 OIT 把对象视图物化到新容器。源容器不用停写，因为读的是冻结视图。这和 RBD clone 不同：RBD 是 parent 引用 + 对象 COW，我们是按 epoch 把视图拷出来。POSIX 容器还踩过 CO_ROOTS：DFS 会给新容器生成一套新的超级块 OID，对象却按原 OID 拷，树就对不上，修的是克隆时把源容器的 CO_ROOTS 抄过去、跳过 DFS 初始化。

**展开 · 回滚**

语义：容器原地回到某个**已经存在的持久快照**，之后的数据永久丢掉，比它新的快照全删，所有打开句柄强制踢掉。破坏性操作，跟非破坏的 clone 互补。上游这个 API 原来直接 `NOSYS`，我们从 CLI 打到 VOS 整条做的。

口述五层，别背函数名，讲因果：

1. CLI：`daos container rollback`，指定 epoch 或快照名。
2. 客户端：走现成的 epoch-op 通道，复用快照创建/删除那条 RPC 管线。
3. MS 同一个 Raft 事务里：校验这个 epoch 真的是持久快照（不让回滚到任意时间点）→ **先驱逐全部句柄**（否则回滚中途还有人在写）→ 广播到所有 engine → 成功后再裁 RDB：删掉比回滚点新的快照和对应 OIT。广播失败事务不提交，元数据零变化。
4. 每个 engine 的每个 target 线程：停这个容器的后台聚合 → `vos_discard` 丢掉 `(rollback_epoch, MAX]` 这段版本 → 裁内存快照表 → 再开聚合。discard 碰到活跃事务会退避重试。
5. 为什么 discard 而不是拷旧数据：旧版本本来就在，正确动作是删未来，复杂度跟「回滚点之后的增量」成正比，不是跟容器容量成正比，而且幂等，重试安全。

为什么停聚合：聚合在合并旧版本，discard 在删新版本，同时碰一棵 VOS 树会打架。跟 RBD rollback 对比可以主动讲：RBD 是把快照对象拷回镜像头部，O(镜像大小)；我们是丢弃式，O(增量)。这就是对象 COW 和 epoch MVCC 在同一个功能上的实现差。

---

## 17. GMP 调度模型

**开场**

Go 的调度就三个字母：G 是 goroutine，M 是内核线程，P 是逻辑处理器。M 必须绑上一个 P 才能跑 G。P 的数量默认等于 `GOMAXPROCS`，它限制的是真正的并行度，不是 goroutine 数量。

**展开**

G：用户态协程，初始栈大约 2KB，不够再涨。你 `go func()` 就是造一个 G。

M：真正被操作系统调度的线程。没有 M，G 就是个数据结构，跑不了。

P：可以理解成「一把能跑 Go 代码的座位」，上面挂着本地可运行队列，还有内存分配的本地缓存。座位就那么多，所以 CPU 密集代码跑不满核，先看 GOMAXPROCS。

调度怎么找活：新 G 优先进当前 P 的本地队列，无锁，快；队列满了才进全局队列。某个 M 闲了，顺序大概是：本地队列 → 全局队列 → 网上等着的（netpoller）→ 去别的 P 偷一半（work-stealing）。偷是为了别让有的核忙死、有的核空转。

阻塞怎么处理，这是精髓：

- G 进了阻塞系统调用（比如同步文件 IO），M 会陪它陷进内核，但 **P 会立刻解绑**，交给别的 M（或新建一个）继续跑这个 P 队列里的其他 G。所以一个阻塞 syscall 不会把整个调度器卡死。
- 网络 IO 更聪明：netpoller 把 fd 丢给 epoll，G 挂起，M 根本不跟着睡，去跑别的 G。所以 Go 网络程序可以几十万 goroutine，真正的内核线程就几十个。

抢占：老版本纯协作，一个 G 死循环不函数调用，会卡住这个 P。Go 1.14 起用信号做异步抢占，纯计算也能被打断。后台还有个 sysmon，盯着跑太久的 G，回收陷在 syscall 里太久的 P。

跟存储工作的关系可以说两句：我们控制面、客户端是 Go，导入导出用多 goroutine 并行拷、`LockOSThread` 绑核——那是故意把 G 钉死在 M 上，为了跟 NVMe 队列亲和，等于局部不用 GMP 的迁移能力。知道默认模型，才知道什么时候该打破它。

被追问「G 能开多少」：内存和你的阻塞类型决定，不是调度器先垮。每个 G 栈加它引用的对象才是上限；网络等待型可以非常多，CPU 密集开再多也就是 P 那么宽。

---

## 口述节奏建议

1. 每题先 20 秒结论，看面试官点头再展开。
2. 能接到 DAOS / Seastar / 导入导出 / 会话 COW 的，留在最后一句，别一上来讲项目，显得套话。
3. 第 16 题是你的主场，时间不够就砍别的细节，这块要讲完「MVCC 钉 epoch」和「discard 未来」两个原语。
4. 第 4、5、13、15 题是一套：内存序 → 无锁环 → io_uring → 对比 epoll。串起来讲比单题背分高。
